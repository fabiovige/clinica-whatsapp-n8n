-- Banco da Evolution API (criado automaticamente pelo docker-entrypoint-initdb)
-- Nada aqui — Evolution cria as próprias tabelas automaticamente.
-- Esse arquivo existe só pra satisfazer o volume mount.

SELECT 'Evolution DB initialized' AS status;
